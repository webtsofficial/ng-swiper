import { SwiperItemDirective } from './swiper-item.directive';

describe('SwiperItemDirective', () => {
  it('should create an instance', () => {
    const directive = new SwiperItemDirective();
    expect(directive).toBeTruthy();
  });
});
